for o in range(1,7): # < loop 1 to get the first dice value
    for t in range(1,7): # < loop 2 to get the seconud dice value
        print(str(o) + "," + str(t)) # < print it as a pair and converts the integer to a string
